spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/qpq-test
    username: postgres
    password: sopra*
    driverClassName: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
liquibase:
  enabled: false